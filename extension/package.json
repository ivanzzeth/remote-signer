{
  "name": "remote-signer-extension",
  "version": "0.3.4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "prebuild": "npm install",
    "test:e2e": "npm run build && playwright test --grep-invert @live",
    "test:integration": "playwright test --grep @integration",
    "test:live": "LIVE_DAPP_E2E=1 npm run build && LIVE_DAPP_E2E=1 playwright test --grep @live"
  },
  "dependencies": {
    "remote-signer-client": "file:../pkg/js-client"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "@playwright/test": "^1.52.0"
  }
}
