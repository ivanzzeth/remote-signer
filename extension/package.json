{
  "name": "remote-signer-extension",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "prebuild": "npm install"
  },
  "dependencies": {
    "remote-signer-client": "^0.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
