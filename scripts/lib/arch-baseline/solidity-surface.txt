# solidity 规则引擎的规模。⛔ 只许往下改 —— 变小也红,那一刻正是把数字改小的时候。
#
# ✅ 2026-09-10:rules/ 里的 solidity 规则声明 **73 → 0**。
#    九个模板(polymarket_* / predict_* / opinion_safe)和四个顶层规则文件已删除 ——
#    它们没有任何 preset 引用,而 solidity 自 2026-09-09 起默认关闭,所以那些文件
#    在出厂配置下本来就装不上。两个 example 文件里的规则改写成了 evm_js,
#    因为「只有先打开一个默认关闭的引擎才能跑」的示例不是示例。
#
# ⛔ 引擎本身(下面这 11 个文件 / 4162 行)**还在,而且这是有意的**:
#    删掉它是**移除一项能力**,不是重构。任何 opt-in 打开
#    chains.evm.foundry.enabled 并自己写了 evm_solidity_expression 规则的部署
#    都会因此中断,而那不是能从这个仓库里看出来的事。
#
#    但对**每一份出厂配置**来说它现在已是死重:0 条规则、0 个 preset、默认关闭。
#    真要删,那是一次提交的事(18 个文件的 wiring + internal/chain/evm/solidity_*),
#    等一个明确的决定。
#
# ⛔ rules_declarations 保持 0:再往 rules/ 里加一条 solidity 规则,就是让出厂
#    配置重新依赖一个默认关着的引擎。

solidity_lines         4162   # arch_prod_files internal | grep solidity 的总行数
solidity_files           11   # 同上的文件数
rules_declarations        0   # rules/ 下 `type: evm_solidity_expression` 的声明数
presets_needing_forge     0   # 引用了带 solidity 模板、因而默认配置下装不上的出厂 preset
