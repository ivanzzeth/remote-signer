# solidity 规则引擎的规模。⛔ 只许往下改 —— 变小也红,那一刻正是把数字改小的时候。
#
# 方向已定:evm_js(sobek,进程内,不 fork)取代 evm_solidity_expression。
# 2026-09-09 已把 solidity 改成 opt-in、默认关。棘轮保证这次迁移不会停在半路,
# 更不会反向长回去。
#
# ⚠️ rules_declarations 这个数字有个刺眼的含义:出厂配置默认关着 solidity,
# 而 rules/ 里还有 73 条真实的 solidity 规则声明 —— 带它们的模板在默认配置下装不上。
# presets_needing_forge 是那件事的终点线:降到 0,solidity 引擎才真的能删。
#
# 今天的 3 个:polymarket_eoa_polygon / polymarket_v2_eoa_polygon / predict_eoa_bnb。
# 其余 25 个出厂 preset 已经是纯 evm_js;predict_eoa_bnb 甚至已经有 _js 版可参照。

solidity_lines         4162   # arch_prod_files internal | grep solidity 的总行数
solidity_files           11   # 同上的文件数
rules_declarations       73   # rules/ 下 `type: evm_solidity_expression` 的声明数(不是提及数)
presets_needing_forge     3   # 引用了带 solidity 模板、因而默认配置下装不上的出厂 preset
