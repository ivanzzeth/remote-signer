# ⑪b 超过 1 MB 的已跟踪文件 —— 基线,由 scripts/arch/40-no-tracked-binaries.sh 读取。
#
# ⭐ 今天是空的。当前最大的已跟踪文件是 app/metamask-snap/package-lock.json,
# 459 KB —— 离 1 MB 还有一倍余量,所以这条门禁今天不会误伤任何东西。
#
# ⚠️ 这条和 ⑪a 抓的不是一类东西,别合并:压缩过的 JS bundle 是**文本**,
# git 不当它二进制,⑪a 放它过去;而一个 500 KB 的 ELF 过得了 1 MB 这条。
# 两条各自漏掉对方抓的那一半。
#
# 格式:一行一个仓库相对路径,`#` 之后是注释,每行写明为什么它在这儿。
