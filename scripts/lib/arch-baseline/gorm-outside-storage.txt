# 直接 import gorm.io 的非存储层文件。
#
# 后果:换存储引擎、加读写分离、改事务边界 —— 全都要动这些包,而 internal/storage
# 那层接口本来就是为了不让这种事发生。
#
# ⛔ 只许缩。新增一行 = 又一个包被焊死在 GORM 上。

internal/api/handler/preset.go     # preset apply 要事务:tx *gorm.DB 一路传进 handler
internal/api/router.go             # wiring 里拿 *gorm.DB 造 repo
internal/chain/evm/token_metadata.go  # 🔴 链适配器直接读写 DB —— 最不该的一处
internal/cli/server/run_init.go    # bootstrap:开库
internal/cli/server/run_router.go  # bootstrap:把 db 传给 router
internal/cli/server/run_settings.go   # bootstrap:settings store 要 db
internal/cli/server/server_registry.go  # bootstrap
internal/settings/store.go         # settings 自带一份 GORM 存储实现
