# coverage_boost 测试的规模。⛔ 只许往下改。
#
# 这些文件的名字直说了存在理由是抬覆盖率数字,不是描述行为。后果:它们的失败
# 信息读不出「什么坏了」,只读得出「某一行没被走到」——于是没人修,只会被注掉。
#
# ⛔ 审计里「确认无独占覆盖后删掉」这条**执行不了**,实测(go test -cover,
#    移走全部 boost 文件前后):
#
#      internal/api/handler        92.0% → 89.1%
#      internal/api/handler/evm    71.4% → 53.5%
#      internal/chain/evm          83.4% → 70.4%
#
#    17.9 个点不是噪声,是别处没有的错误路径覆盖。整体删除会掉真覆盖。
#
# ✅ 2026-09-10 已完成第一步(共享 helper 归位):
#    chain/evm 那一行此前是「**编译失败**」—— mockPasswordProvider 与
#    transferCalldata 定义在 boost 文件里,而普通的 provider / solidity 测试依赖
#    它们。也就是说这些文件同时是该包的测试基础设施,删不掉不是因为覆盖率,
#    是因为别的测试编译不过。两个 helper 已搬进 shared_test_helpers_test.go,
#    现在移走 boost 文件那个包能正常编译并跑。
#
# 剩下两步(都让下面的数字变小,且都不掉覆盖):
#   · 能说清测的是什么行为的 → 改名搬去对应的 _test.go
#   · 说不清的 → 才删
#
# ⚠️ 变小也红 —— 那一刻正是把基线改小的时候(一行编辑)。
#
# 2026-09-10 · 提案 S4(signers 拆解):**15677 → 15664 行**,文件数不变。
# 少掉的 13 行是 `TestB3HandleRevokeAccess_NoKeyInPath`:它断言
# `DELETE /api/v1/evm/signers/{addr}/access`(没有 key id)回 400
# "api_key_id is required in path" —— 而那道守卫**只因为一条方法无关的前缀
# 让「没有 key id」的请求进得了 handler 才存在**。拆解之后 `{keyID}` 通配段
# 不匹配空段,请求根本到不了 handler,那句话再也产生不出来。
# ⛔ 不是删掉了事:它按「说得清测的是什么行为的就改名搬走」那条路走的 ——
# 现在是 signer_routes_test.go 里 TestSignerRoutes_UnclaimedPathsReachNoHandler
# 的一行,断言的是同一个请求**到不了任何 handler**(经生产 pattern 判定)。

# 2026-09-11 · 提案 S5(settings 拆解):**15664 → 15612 行**,文件数不变。
# 少掉的 52 行是 `errorSettingsStore` 与 `TestCoverage_Settings_Put_Error`,
# 它们**搬到了 internal/api/handler/settings_test.go**,名字与断言一个字没改。
# ⛔ 不是删掉了事:走的正是上面那条「能说清测的是什么行为的 → 改名搬去对应的
# _test.go」——它测的是九个 group 的 PUT 在 store 写失败时回 500,是 settings
# 的行为,和 coverage_boost 里那些 api-key 的私有方法调用没有关系。
# ⚠️ 搬家的**触发**恰恰是这条棘轮:S5 先是把那张表从「路径 → ServeHTTP」改成
# 「路径 + 端点函数」,coverage_boost 涨了 11 行,棘轮报红 —— 提示语写的
# 「别往 coverage_boost 里塞」就是这次该做的事。

files      13
lines   15612
