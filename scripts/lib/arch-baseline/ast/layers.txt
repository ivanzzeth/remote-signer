# Clean Architecture 依赖方向的违规边(importing-pkg -> imported-pkg)。
# 由 cmd/archcheck 用 AST 读 import 得出;层的定义在 cmd/archcheck/layers.go。
#
# ✅ 2026-09-10:13 → 8。五条 core/* -> internal/storage 全部消除。
#    修法:仓储接口、它们的查询结构、以及调用方要匹配的哨兵错误,搬进
#    internal/core/ports —— **由使用它们的那一层声明**;internal/storage 实现,
#    并用 `type X = ports.X` 别名回来,所以几百处 storage.RuleRepository 调用
#    一行都没改,而每个类型仍然只有一个定义。
#
# ⚠️ 别名必须是 `type X = ports.X`(同一类型两个名字),不能写成 `type X ports.X`
#    —— 后者是新类型,两者会悄悄不再互换。
#
# 剩下的 8 条:
#   · api -> config(3):handler 直接读 config.Config,而不是拿装配好的值
#   · core/service -> audit/notify/chain/chain-evm(4):领域层认识了具体实现,
#     其中 chain/evm 那条最贵 —— 加第二条链要改领域层
#   · core/statemachine -> audit(1)
#
# ⛔ 只许缩。⛔ 也不许通过放宽 layers.go 的 MayImport 让某条消失。

internal/api -> internal/config
internal/api/handler -> internal/config
internal/api/middleware -> internal/config
internal/core/service -> internal/audit
internal/core/service -> internal/chain
internal/core/service -> internal/chain/evm
internal/core/service -> internal/notify
internal/core/statemachine -> internal/audit
