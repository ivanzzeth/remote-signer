# Clean Architecture 依赖方向的违规边(importing-pkg -> imported-pkg)。
# 由 cmd/archcheck 用 AST 读 import 得出;层的定义在 cmd/archcheck/layers.go。
#
# ✅ 2026-09-10:13 → 0。这张表现在是空的。
#
# 走过的路,按顺序:
#   13 → 8  仓储接口、查询结构、哨兵错误搬进 internal/core/ports,
#           由 internal/storage 实现并以 `type X = ports.X` 别名回来。
#           五条 core/* -> storage 一次消除。
#    8 → 7  SeverityForEvent 搬到 core/types —— 它是事件类型的分类,
#           不是审计设施的行为。
#    7 → 5  SignAuditSink(2 个方法)与 Notifier + NotifyChannel:
#           service 此前为了调 Send 而依赖整个通知传输层。
#    5 → 4  ChainRegistry / SimulationBudgetEvaluator / SignerInventory。
#           SimulationOutcome 只保留 Decision 与 Reason 两个字段 —— 签名路径
#           只读这两个,完整结果属于仿真引擎和渲染它的 UI。形状不同,所以
#           evm 侧提供 SimulationBudgetPort 做映射(adapter -> usecase 是
#           允许方向)。
#    4 → 3  ReceiptFetcher(1 个方法),取代整个 RPCProvider。
#    3 → 0  IPWhitelistConfig 搬进 ports —— 它是地址列表加两个开关,
#           delivery 层为了判断放不放行,此前要 import 组装根。
#
# ⛔ 保持 0。⛔ 尤其不许通过放宽 layers.go 的 MayImport 让某条消失 ——
# 那是把尺子改短来让人变高。真要新增一条,先问:内层需要的那个东西,
# 能不能由内层声明成接口,让外层去满足?上面六步全是这一个动作。
