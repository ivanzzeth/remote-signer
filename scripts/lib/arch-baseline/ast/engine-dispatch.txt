# 调用方自己判断「这条规则用哪个引擎」的地方。
#
# 求值侧已经是干净的多态:RuleEvaluator 有 Type() + Evaluate(),进注册表,
# 加引擎不用改任何调用点。验证侧没做这一步,三个 validator 签名各不相同:
#   JSRuleValidator.ValidateRule(ctx, script, testCases)
#   SolidityRuleValidator.ValidateRule(ctx, rule)
#   MessagePatternRuleValidator.ValidateRule(ctx, rule)
# 于是每个调用方只能手写分支。
#
# 判据:一个文件点名 ≥2 个不同的 RuleTypeXxx 常量。点名 1 个是引擎在声明
# 自己的 Type(),合法;常量声明处 internal/core/types 豁免。
#
# ⛔ 收敛方向是让引擎自己回答问题,⛔ 永远不是删掉某个引擎。
#
# ── 这些是注册表:加引擎在这里加一行是应付的成本,不是要消掉的债 ──
internal/chain/evm/rule_evaluator.go
internal/cli/server/server_validate.go
internal/cli/server/server_validate_evm.go
internal/core/rule/generator.go
internal/ruleconfig/validate.go
#
# ── 这些是调用方在问引擎问题,是要消掉的 ──
#
# 2026-09-10: 9 → 6。
#   internal/validate        → ValidRuleTypes 改为从 types.RuleTypes() 派生
#   rule_crud.go             → 问 TakesTestCases / RequiresToolchain,并删掉
#                              两处重复的 evm_js 校验(ValidateRuleConfig 更严)
#   rule.go                  → RBAC 两张表改为从 ExecutesArbitraryCode /
#                              GovernsSignerAccess 派生,从 fail-open 变 fail-closed
#
# 只剩 validate_rules.go 一个真调用方,它是三个签名不同的 validator 之间的
# switch —— 那需要 RuleValidator 接口(照抄 RuleEvaluator + BatchRuleEvaluator
# 那对已经存在的组合),不是加个描述符字段能解决的。
internal/cli/validate/validate_rules.go
