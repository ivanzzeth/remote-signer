# 在构造期把 settings 开关冻成结构体字段的地方(包.结构体.字段)。
# 由 cmd/archcheck 读 settings.SecuritySnapshot 的字段表 + 扫 internal/api 的
# 结构体字段得出 —— 快照加字段时这张表会自动跟着长,不用手抄。
#
# ✅ 2026-09-10:21 → 2。所有真正的开关都解冻了 —— handler 持有
# func() bool / func() int / func() time.Duration,由 Router.liveReadOnly /
# liveInt / liveDuration 生成的闭包在**每次请求**时读快照。
# 行为由 internal/api/handler/readonly_live_test.go 钉住。
#
# ⚠️ 只认标量类型(bool/int/string/time.Duration/[]string)。同名但持有服务对象的
# 字段不算 —— RouterConfig.ApprovalGuard 是 guard 本身,而且它一直是活的
# (router.syncApprovalGuard 会回写)。
#
# 剩下两条是 **/health 的响应 DTO 字段**,不是被冻住的开关:它们由
# securityConfigNow() 每次请求从快照重算(格式化规则见 applySecurityDurations),
# 结构体字段只是响应体的形状。检查按字段名匹配,分不出「持有配置」和
# 「报告配置」,所以它们留在这里,理由写在这。
#
# ⛔ 新增一行 = 又有人把一个运行时可改的开关拷进了结构体。

internal/api/handler.SecurityConfigInfo.AutoLockTimeout
internal/api/handler.SecurityConfigInfo.SignTimeout
