# 少数派参数顺序的 write helper。
#
# ✅ 已清零(2026-09-09)。此前 writeJSON 有 (w, any, int) 22 份 和 (w, int, any)
# 5 份;writeError 有 (w, string, int) 19 份 和 (w, int, string) 3 份。
# **两种都编译得过** —— 传反了不报错,线上表现是 200 里塞着错误体。
# 8 个少数派已统一到多数派顺序,那也是标准库的顺序(http.Error(w, msg, code))。
#
# ⛔ 保持 0。下一步是把这 48 个方法收进共享的 respond 包 —— 但顺序统一是前提,
# 不然「抽一个共享实现」这件事本身就没法做。
