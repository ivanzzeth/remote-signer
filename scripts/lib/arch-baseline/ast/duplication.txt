# 形状高度相似的函数对(归一化后 ≥88%,各 ≥30 行)。
#
# ⚠️ 相似不等于重复。两个函数可以长得像而意思不同,把那种合并会更糟 ——
#    这种对就留在这份基线里,写清理由。棘轮禁止的是**数量变多**。
#
# 判据怎么定的(实测,不是拍脑袋):
#   最小行数 15 → 145 对,其中 31% 是 TUI 框架样板(SetSize/View/newXModel,
#                 Bubble Tea 每个 model 都得那样写,结构相同是设计如此)
#   最小行数 20 → 49 对,24% 样板
#   最小行数 25 → 11 对,18% 样板
#   最小行数 30 →  7 对, 0% 样板   ← 选它
# 归一化会抹掉标识符、字符串和数字,所以「改了循环变量名的副本」照样算 100%。
#
# 这道门禁上线当天,靠它之前的手工扫描抓到的:
#   - AddDelegationTargets 三份,其中一份把无法解析的配置读成「不委托」,
#     隔离引擎因此跳过整条委托链,给出一个假的 pass
#   - PresetRegistry.Sync / TemplateRegistry.Sync 只差循环变量名和日志名词,
#     而那段代码会**删目录行**
#   - Solidity 四个 shape 各写一套生成器和求值器,两个 typed_data 还各抄了
#     同样 7 个 EIP-712 域绑定
#
# 2026-09-10:7 → 6。NewDB 是 NewDBWithLogger 的 50 行副本,只差一个
#   logger.Silent —— 而两份里都写着「检测方言 → 打开 → 调连接池 → automigrate
#   → 跑版本化迁移 → 补外键 → 修历史时间戳」。哪天有人往其中一份加一步迁移,
#   数据库就取决于二进制调了哪个构造函数。
#
# 2026-09-10:6 → 5。两个 annotate 合成 annotateBudget —— 连结尾那段
#   「谁能看见这一行」的所有权判断都是逐字两份,而那正是最会漂成「一个 handler
#   泄露另一个 handler 藏起来的东西」的代码。两份已经在外观上漂了:一份用
#   time.RFC3339,另一份把同一个 layout 手写了一遍。
#
# 2026-09-10:5 → 4。两个 List(FileTemplateSource / FilePresetSource)合成
#   泛型 listYAMLTree —— 逐字两份的 stat + 容忍目录不存在 + 拒绝非目录 + 走树 +
#   过滤扩展名。容忍不存在的那个分支才是关键:它是「新装的机器还没写 presets」
#   能正常启动的原因,哪份副本掉了它,一边的目录会把空目录读成启动失败,另一边
#   照常。两份已经在文档上漂了:模板那份的注释写着「解析失败不中断遍历,跳过该
#   文件」,而代码其实是 return err 中断整棵树 —— 注释描述的是早就不存在的行为。
#
# 2026-09-10:4 → 3。两个 Upsert(模板 / preset)合成泛型 upsertCatalogueRow,
#   两张表各自的差异收进一个 fresh 谓词。preset 那份的注释写着「Mirrors
#   TemplateRepository.Upsert exactly」—— 而它不是:模板那份的跳过条件除了
#   content_hash 还比 type/mode(用来修老版本写进去的 type="" 的 bundle 行),
#   preset 那份只看 hash。差异本身是对的(RulePreset 没有 type/mode),错的是
#   那句「exactly」:下一个照着 preset 抄第三张表的人会连模板的修复逻辑一起漏掉。
#   selectCols 与 fresh 现在挨着写 —— 投影里漏一列,那列读回来是零值、永远比不
#   相等,跳过分支会退化成每次启动整表 UPDATE。
#
# 2026-09-10:3 → 2。doRPC / doRPCUnchecked / doRPCRaw 三份(门禁只配到其中
#   两份,doRPCRaw 是它没配上的第三份)合成一条 rpcRoundTrip:熔断 → 限流 →
#   组包 → POST → 限长读 → 查状态 → 解信封 → 报 JSON-RPC 错。三份副本各自写了
#   一遍 recordError —— 而每一处 recordError 都是给整个 provider 的熔断器投票,
#   哪份漏掉一条失败路径,那条路径就永远打不开熔断器,上游已经挂了守护进程还在
#   照打。recordSuccess 故意留在外层:解字符串失败要算失败,放进 rpcRoundTrip
#   的话「200 但结果形状不对」会先清零再加一,永远到不了阈值。
#   两份已经在外观上漂了:doRPC 的熔断文案多一句「using cached data only」,
#   而缓存是 TokenMetadataCache 那一层的事,provider 自己并不用缓存 —— 统一成
#   不带那句的准确文案。
#
# 2026-09-10:2 → 0。四个通知客户端的扇出(Slack / Telegram / Pushover /
#   webhook —— 门禁只配到前三个中的两对)合成一个 fanOut。要守住的是「部分失败」
#   那条规则:三个频道里挂了一个返回 nil(人已经收到了,只是没收全),三个全挂
#   才返回 error —— 上层的升级逻辑就认这一点。哪份副本把这两半中的任意一半写反,
#   要么丢掉一条没人会发现缺失的告警,要么把一条其实已经送达的告警报成失败。
#   四份在成功路径的日志上已经漂了:Telegram 每条 debug + 全成功 info,webhook
#   每条 debug,Slack 与 Pushover 成功时一个字都不写 —— 运维在日志里看得见
#   Telegram 发成功,看不见 Slack 发成功。现在统一成 Telegram 那份(只增不减)。
#
# 这份基线现在是空的。⛔ 空不等于这道门禁没用了:它禁止的是数量变多,
#   下一个 ≥88%、≥30 行的新副本会当场把 make check 判红。
#
# ⛔ 收敛方向是「同一个形状只留一份实现,两边都用它」。
#
