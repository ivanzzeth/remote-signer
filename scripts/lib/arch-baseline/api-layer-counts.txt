# API 层的计数基线。⛔ 只许往下改 —— 实际数字**变小也红**,那一刻正是把这里
# 改小的时候(一行编辑)。放过它,基线就永远停在今天的高位。
#
# 用计数而不是文件集合的理由:这四条的重构方式是「一次抽干净」,过程中数字会
# 大幅下跌;用集合的话每改一个文件都要重写整张表,那种门禁人会绕过。

write_helpers             1   # ✅ 2026-09-10:48 → 1。48 份实现收进 internal/api/respond;
                               # 剩下的 1 个是 rpc_proxy 的 writeRPCError —— 它包的是
                               # JSON-RPC 错误信封(HTTP 200 + envelope),不是通用响应,
                               # 收编进去会让 respond 包知道一个它不该知道的协议。
router_config_fields     37   # RouterConfig 的字段;它是服务定位器,不是配置。
                               # 2026-09-10:40 → 37,三个 *_api_readonly 开关不再冻在这里
manual_method_checks     35   # 手写 r.Method != http.MethodX
