# API 层的计数基线。⛔ 只许往下改 —— 实际数字**变小也红**,那一刻正是把这里
# 改小的时候(一行编辑)。放过它,基线就永远停在今天的高位。
#
# 用计数而不是文件集合的理由:这四条的重构方式是「一次抽干净」,过程中数字会
# 大幅下跌;用集合的话每改一个文件都要重写整张表,那种门禁人会绕过。

write_helpers             1   # ✅ 2026-09-10:48 → 1。48 份实现收进 internal/api/respond;
                               # 剩下的 1 个是 rpc_proxy 的 writeRPCError —— 它包的是
                               # JSON-RPC 错误信封(HTTP 200 + envelope),不是通用响应,
                               # 收编进去会让 respond 包知道一个它不该知道的协议。
router_config_fields     29   # RouterConfig 的字段;它是服务定位器,不是配置。
                               # 2026-09-10:40 → 29。十个运行时可改的开关改为闭包实时读快照;
                               # bootstrap 与 transactions 改成 feature module
                               # (internal/api/module.go),由组装根构造并传进
                               # RouterConfig.Modules —— 它们的依赖不再是 router 的字段。
                               #
                               # ⭐ 新功能加 Module,不要加字段。剩下 29 个字段里每一个都是
                               # 「router 得知道它才能决定某条路由存不存在」,这正是读
                               # setupRoutes 判定不了线上服务什么的原因。
manual_method_checks      6   # 手写的 method 比较(判据是对生产文件的一次 grep)
                               # ✅ 2026-09-10:35 → 7。二十余条路由改成按方法注册
                               # (`GET /api/v1/audit`、`POST /api/v1/evm/sign`、
                               # `POST /api/v1/evm/signers/{address}/unlock` …),
                               # Go 的 ServeMux 自己回 405,handler 里那些判断随之
                               # 变成死代码。忘了写一条 mux 做不到。
                               #
                               # ✅ 2026-09-11:7 → 6。提案 S6 后半把
                               # `/api/v1/templates` 与 `/api/v1/templates/` 两条
                               # 不带 method 的前缀拆成八条具名路由,
                               # TemplateHandler.ServeHTTP 连同它 validate 分支里
                               # 那一处比较一起消失。
                               #
                               # 剩下 6 处都在**按路径分发的闭包**里
                               # (/api/v1/evm/requests/ 在一个 handler 内部再按
                               # 子路径分派),要消掉它们得先把那个闭包拆成按子路径
                               # 注册的路由 —— 那是提案 S7,见 router_config_fields。
                               #
                               # ⛔ 判据是 `grep 'r.Method != http.Method'`,**注释也算**。
                               # 在生产文件里把这串字面量写进散文(比如解释「这里以前有
                               # 一处」)会让这个棘轮停在旧值上,而且一个字的红都不报 ——
                               # 2026-09-11 实测撞到过一次。
