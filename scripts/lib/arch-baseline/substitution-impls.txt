# 定义 `${var}` 代换函数的文件。
#
# 🔴 全仓库最危险的一条重复。不是可读性问题 —— **校验期用严格实现(报错),
# 求值期用宽松实现(永不报错)**,两者对同一个 `${var}` 的处理只要有一处不同,
# 结果就是「test_case 全绿的规则,运行时授权了另一件事」。
#
# 各份的语义(2026-09 审计):
#   core/service/substitute.go        SubstituteTyped —— 类型感知、JSON 编码、报错
#   core/rule/effective_config.go     substituteConfigVars —— 求值期、best-effort、不报错
#   chain/evm/effective_config.go     SubstituteTestCaseInput —— 测试用例输入
#   preset/preset.go                  substituteMapVars / substituteBudgetMapVars
#   config/rule_init_budget.go        substituteBudgetMapVars —— **同名不同签名**
#
# ⛔ 只许缩。新增一行 = 又一份可能与校验期漂开的求值语义。

internal/chain/evm/effective_config.go
internal/config/rule_init_budget.go
internal/config/template_init.go
internal/core/rule/budget_unit.go
internal/core/rule/effective_config.go
internal/core/service/substitute.go
internal/preset/preset.go
