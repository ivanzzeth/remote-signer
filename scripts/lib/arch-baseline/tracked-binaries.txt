# ⑪a 已跟踪的二进制文件 —— 基线,由 scripts/arch/40-no-tracked-binaries.sh 读取。
#
# ⭐ 这份清单**今天是空的**,而且应该一直是空的。
#
# 2026-09-10 之前它不空:`archcheck`(3.8 MB,cmd/archcheck 的编译产物)在库里
# 躺了 8 个版本。清理它的代价是重写两个仓库的全历史、force-push main 和
# 32 个 tag、改写父仓库 41 个 submodule 指针。加一行到这个文件只要 5 秒,
# 所以⛔ 加之前先确认:它真的必须入库吗?
#
# 什么算「真的必须」:测试用的固件/样本二进制、字体、没有文本等价物的资源。
# 什么不算:任何 `make` / `go build` / `npm run build` 能重新生成的东西 ——
# 那些进 .gitignore。
#
# 格式:一行一个仓库相对路径,`#` 之后是注释。**每一行都必须写明为什么它在这儿** ——
# 没有理由的条目,下一个人不知道能不能删,于是永远不删。
