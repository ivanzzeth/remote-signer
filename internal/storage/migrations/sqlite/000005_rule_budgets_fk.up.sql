-- FK constraint on rule_budgets.rule_id -> rules(id) ON DELETE CASCADE is
-- applied by ensureForeignKeys() after AutoMigrate+migrations complete.
-- This migration is a placeholder so the version sequence is recorded.
SELECT 1;
